# 📘 Métodos e Funções em C#

**Resumo da Aula:**
Este repositório contém os exemplos práticos da, focada no entendimento, construção e aplicação de métodos e funções na linguagem C#. O objetivo principal é demonstrar como encapsular regras de negócio, promover o reaproveitamento de código e manter a legibilidade do software. Os exemplos avançam desde assinaturas simples até a integração de métodos com coleções e estruturas de controle.

**Conceitos e Exemplos Abordados:**
* **Métodos sem retorno (`void`):** Execução de blocos de código sem devolução de estado (ex: `ExibirMensagem()`).
* **Métodos com retorno (`int`, `bool`):** Processamento de dados e devolução de resultados (ex: `Somar()`, `EhPar()`).
* **Passagem de Parâmetros:** Como injetar dados externos para dentro do escopo da função (ex: `Saudacao(string nome)`).
* **Integração com Estruturas de Controle (`if/else`):** Tomada de decisão isolada dentro do método (ex: `VerificarNumero()`).
* **Integração com Laços de Repetição (`for`, `while`, `do-while`):** Iterações contidas no escopo funcional (ex: `ExibirTabuada()`).
* **Manipulação de Coleções (`List<T>`):** Passagem de listas genéricas como parâmetro e iteração com `foreach` (ex: `ExibirTarefas()`, `ExibirNumerosESoma()`).

---

## 🇺🇸 English

**Lesson Summary:**
This repository contains practical examples, focused on understanding, building, and applying methods and functions in C#. The main goal is to demonstrate how to encapsulate business rules, promote code reuse, and maintain software readability. The examples range from simple method signatures to integrating methods with collections and control flow structures.

**Concepts and Examples Covered:**
* **Void Methods (`void`):** Executing code blocks without returning a state (e.g., `ExibirMensagem()`).
* **Methods with Return Types (`int`, `bool`):** Processing data and returning results (e.g., `Somar()`, `EhPar()`).
* **Parameter Passing:** Injecting external data into the function's scope (e.g., `Saudacao(string nome)`).
* **Integration with Control Structures (`if/else`):** Isolated decision-making within the method (e.g., `VerificarNumero()`).
* **Integration with Loops (`for`, `while`, `do-while`):** Iterations contained within the functional scope (e.g., `ExibirTabuada()`).
* **Collection Handling (`List<T>`):** Passing generic lists as parameters and iterating with `foreach` (e.g., `ExibirTarefas()`, `ExibirNumerosESoma()`).

---

## 🇪🇸 Español

**Resumen de la Clase:**
Este repositorio contiene los ejemplos prácticos, enfocada en comprender, construir y aplicar métodos y funciones en C#. El objetivo principal es demostrar cómo encapsular reglas de negocio, promover la reutilización de código y mantener la legibilidad del software. Los ejemplos abarcan desde firmas simples hasta la integración de métodos con colecciones y estructuras de control.

**Conceptos y Ejemplos Cubiertos:**
* **Métodos sin retorno (`void`):** Ejecución de bloques de código sin devolver un estado (ej: `ExibirMensagem()`).
* **Métodos con retorno (`int`, `bool`):** Procesamiento de datos y devolución de resultados (ej: `Somar()`, `EhPar()`).
* **Paso de Parámetros:** Cómo inyectar datos externos en el alcance de la función (ej: `Saudacao(string nome)`).
* **Integración con Estructuras de Control (`if/else`):** Toma de decisiones aislada dentro del método (ej: `VerificarNumero()`).
* **Integración con Bucles (`for`, `while`, `do-while`):** Iteraciones contenidas dentro del alcance funcional (ej: `ExibirTabuada()`).
* **Manipulación de Colecciones (`List<T>`):** Paso de listas genéricas como parámetros e iteración con `foreach` (ej: `ExibirTarefas()`, `ExibirNumerosESoma()`).
